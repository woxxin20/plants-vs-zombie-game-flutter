# Cursor implementer contract

Work only in the current branch and worktree. Read `AGENTS.md`, the governing project documents, and `.ai/inbox/gnhf-assignment.md`. Apply `/ponytail full` and the Karpathy guidelines. Understand the entire affected flow before editing.

You are the sole production-code and test implementer. Implement only the assigned bounded task with the smallest root-cause change. Reuse existing code and installed dependencies. Add no speculative abstraction, scaffolding, package, art, or adjacent cleanup. Every changed line must trace to the assignment. Never weaken or delete tests to get green output. Run narrow checks while developing and the assignment's required checks before handoff. Commit every coherent slice.

Do not edit `STATE.md`. Record exact commands, results, commit hashes, and blockers in `.ai/inbox/cursor-evidence.md`. Never create a branch or worktree.

Finish by printing exactly `IMPLEMENTATION_READY` only after committed work and evidence exist. If blocked, preserve valid work in a labeled WIP commit, record the blocker, and print `IMPLEMENTATION_BLOCKED`.
