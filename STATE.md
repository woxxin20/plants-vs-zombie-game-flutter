# STATE — prism-defense

<!-- AGENT-OWNED. Whole-file rewrite only, never patched.
     Humans edit HUMAN NOTES only. Rules: .ai/STATE-PROTOCOL.md -->

CYCLE:   6 (closed)
UPDATED: 2026-09-04T16:20+05:30
BY:      lead:claude
BRANCH:  overnight/gnhf-prism-defense-20260903
COMMIT:  84802ff
STATUS:  NEEDS-REVIEW

## NEXT ACTION
Review diff: `git diff 8e138d8..HEAD` in
`../plants-vs-zombie-game-flutter-gnhf-overnight`.

## PROJECT
Type:    Flutter 3.47 + Flame 1.38 landscape game, offline, no backend
Phase:   PH-02/PH-03 gate review, PH-04 blocked on audio assets
Success: 20 playable levels, 60fps on low-end 720p Android, airplane-mode clean

## GOAL — this branch
Repurpose this repo from the old Plants-vs-Zombie widget game into LIGHT vs
SHADOW — Prism Defense, per LIGHT_vs_SHADOW_Prism_Defense_Flame_Spec.md.
Done when:
- [x] project-governance-kit deployed and all 8 docs filled, zero placeholders
- [x] pubspec retargeted, `flutter pub get` green, old lib/ removed
- [x] pure rules (optics, placement, waves, scoring) written and unit-tested
- [x] `flutter analyze` clean across lib/
- [ ] a level is playable start to win/lose on a device

## BROKEN NOW
- Nothing known in code: analyze clean, 62/62 at `d39b6b0`. Reproduce with
  `flutter analyze && flutter test`.
- Unproven: no level has ever run on a device. Everything below PH-04 is
  gated on the five NEEDS HUMAN items, not on more agent work.

## DECISIONS / DO NOT TOUCH
- Pure gameplay rules stay in lib/data/{optics,rules}.dart without Flame/Flutter imports — keeps them unit-testable.
- hive_ce_flutter replaces hive_flutter (ADR-004); no google_fonts (ADR-005).
- assets/levels/*.json are generated by tool/gen_levels.py with a fixed seed — never hand-edit them.
- Every TapCallbacks consumer imports package:flame/events.dart directly (AUD-011).
- Vector2 has no const constructor; sharing one is safe, Flame copies size/position (AUD-013).
- Hand-painted components taking an OpacityEffect use the FadeableRender mixin (AUD-014).
- No Riverpod state layer (ADR-007) — BattleWorld owns simulation state, HUD components are dumb, SaveStore is the only persistence seam. TASK-021 dropped.
- game.world is NOT camera.world; swapWorld sets the camera's (AUD-017). Reading the wrong one compiles, passes analyze, and silently dead-codes every `is BattleWorld` branch.
- The canonical docs govern; flame-harness's competing docs/harness/ stack was deliberately NOT bootstrapped (owner decision, c6).

## NEEDS HUMAN
- [ ] AdMob ad unit ids and the IAP product id (ARCH-Q-002) — blocks 3 of PH-05's 6 gate items.
- [ ] Native bundle id, still com.example.plants_vs_zombie (ARCH-Q-003) — blocks PH-00's last gate item.
- [ ] The 8 audio files for assets/audio/ — blocks PH-04's audio item entirely (AUD-018 is latent behind it).
- [ ] A device or emulator — blocks PH-00's landscape + 60fps items, PH-02's playthrough, PH-05's profile.
- [ ] Font licensing for Orbitron / Inter / JetBrains Mono (ARCH-Q-001).
- [ ] Keystore / signing identity — blocks PH-06's release builds.

## COST NOTES
- Never read build/ or .dart_tool/ — generated and high-volume.
- Do not read a worker's terminal for progress — an animated TUI prints forever after its stop condition. Read commits and .ai/inbox/. Dispatch cursor-agent with `--print`, which does not do that.
- codegraph is indexed and gitignored: one `codegraph_explore` beats a grep/Read loop and its blast-radius names untested symbols. `codegraph index` fails while the MCP server holds the db — use `codegraph sync`.
- RuFlo MCP is NOT connected in this session (searched by exact tool name and keyword, zero hits) and previously returned zero Dart nodes on this repo. Do not plan around it.
- In widget tests real I/O belongs in setUpAll; Flame's ticker never idles so pumpAndSettle times out.

## HUMAN NOTES
(free text — agent copies this block through byte-for-byte)

## LOG
- 2026-09-04 | c6 | Filled AGENTS.md §6, rewrote README as the doc-ownership map, formatted the tree, reconciled PH-00/PH-01, ADR-007 dropped the never-built Riverpod layer (AUD-016); Cursor delivered PH-01..PH-04 under bounded assignments (62/62); lead review found and fixed AUD-017 (dead lifecycle branch) and logged AUD-018
- 2026-09-04 | c5 | Stopped the stalled GNHF loop (4 exact PIDs) and took the work directly: closed AUD-013, wrote lib/app.dart + boot test, fixed AUD-014 which that first real boot exposed
- 2026-09-04 | c4 | Reconciled orphaned c2; verified analyze=34/tests=34/34; closed AUD-011
- 2026-09-03 | c2 | Root-caused 44/49 analyze errors to tokens.dart's dart:ui TextStyle import; closed AUD-008/AUD-009; opened AUD-012
- 2026-09-03 | c1 | Kit deployed, docs filled, pubspec retargeted, tokens + data + optics + rules + battle world written, 22 unit tests green
