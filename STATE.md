# STATE — prism-defense

<!-- AGENT-OWNED. Whole-file rewrite only, never patched.
     Humans edit HUMAN NOTES only. Rules: .ai/STATE-PROTOCOL.md -->

CYCLE:   6 (open)
UPDATED: 2026-09-04T13:45+05:30
BY:      lead:claude
BRANCH:  overnight/gnhf-prism-defense-20260903
COMMIT:  d01b6c2
STATUS:  GREEN

## NEXT ACTION
Read `.ai/inbox/cursor-evidence-ph02.md`, then re-run `flutter analyze` and
`flutter test` yourself before believing it.

## PROJECT
Type:    Flutter 3.47 + Flame 1.38 landscape game, offline, no backend
Phase:   PH-01 gate review; PH-02..PH-04 delegated and in flight
Success: 20 playable levels, 60fps on low-end 720p Android, airplane-mode clean

## GOAL — this branch
Repurpose this repo from the old Plants-vs-Zombie widget game into LIGHT vs
SHADOW — Prism Defense, per LIGHT_vs_SHADOW_Prism_Defense_Flame_Spec.md.
Done when:
- [x] project-governance-kit deployed and all 8 docs filled, zero placeholders
- [x] pubspec retargeted, `flutter pub get` green, old lib/ removed
- [x] pure rules (optics, placement, waves, scoring) written and unit-tested
- [x] `flutter analyze` clean across lib/
- [ ] a level is playable start to win/lose on a device

## BROKEN NOW
- Nothing known in code: analyze clean, 42/42 at `9cc0c49`. Reproduce with
  `flutter analyze && flutter test`.
- A Cursor worker is mid-flight on PH-02..PH-04 and may have committed since
  `d01b6c2`. Run `git log --oneline d01b6c2..HEAD` before assuming the tree.
- Unproven: no level has ever run on a device. `assets/audio/` is still empty,
  so PH-04's audio gate item cannot be met at all.

## DECISIONS / DO NOT TOUCH
- Pure gameplay rules stay in lib/data/{optics,rules}.dart without Flame/Flutter imports — keeps them unit-testable.
- hive_ce_flutter replaces hive_flutter (ADR-004) — maintained compatible fork.
- No google_fonts dependency (ADR-005) — offline-first forbids runtime fetching.
- assets/levels/*.json are generated by tool/gen_levels.py with a fixed seed — never hand-edit them.
- Every TapCallbacks consumer imports package:flame/events.dart directly (AUD-011).
- Vector2 has no const constructor; sharing one is safe, Flame copies size/position (AUD-013).
- Hand-painted components taking an OpacityEffect use the FadeableRender mixin (AUD-014).
- No Riverpod state layer (ADR-007) — BattleWorld owns simulation state and writes display values onto dumb HUD components; SaveStore is the only persistence seam. TASK-021 is dropped. Do not reintroduce BattleNotifier.
- The canonical docs govern; flame-harness's competing docs/harness/ stack was deliberately NOT bootstrapped (owner decision, c6).

## NEEDS HUMAN
- [ ] Font licensing for Orbitron / Inter / JetBrains Mono (ARCH-Q-001).
- [ ] AdMob ad unit ids and the IAP product id (ARCH-Q-002, PRD-Q-001) — blocks all of PH-05.
- [ ] Native bundle id is still com.example.plants_vs_zombie (ARCH-Q-003) — blocks PH-00's last gate item.
- [ ] The 8 audio files for assets/audio/ — blocks PH-04's audio gate item entirely.
- [ ] A device or emulator: PH-00's landscape + 60fps items and PH-02's on-device playthrough cannot be met without one.

## COST NOTES
- Never read build/ or .dart_tool/ — generated and high-volume.
- Read only relevant ranges of the 1002-line product spec.
- Do not read a worker's terminal for progress — an animated TUI prints forever after its stop condition. Read commits and .ai/inbox/ instead. Dispatch cursor-agent with `--print`, which does not do that.
- codegraph is indexed (`.codegraph/`, gitignored): one `codegraph_explore` call beats a grep/Read loop, and its blast-radius section names untested symbols. Re-run `codegraph init` after big changes.
- In widget tests real I/O belongs in setUpAll, and Flame's ticker never idles so pumpAndSettle times out.

## HUMAN NOTES
(free text — agent copies this block through byte-for-byte)

## LOG
- 2026-09-04 | c6 | Filled AGENTS.md §6 verification contract (7 placeholders), rewrote README.md as the doc-ownership map, formatted the tree, reconciled PH-00/PH-01; ADR-007 dropped the never-built Riverpod layer (AUD-016); Cursor delivered the PH-01 gate suite (42/42, verified test-only) and is now running PH-02..PH-04
- 2026-09-04 | c5 | Stopped the stalled GNHF loop (4 exact PIDs) and took the work directly: closed AUD-013 (adf7676), wrote lib/app.dart + boot test (733fb55), fixed AUD-014 which that first real boot exposed
- 2026-09-04 | c4 | Reconciled orphaned c2; verified analyze=34/tests=34/34; closed AUD-011; assigned AUD-012's smallest sub-slice to Cursor
- 2026-09-03 | c2 | Root-caused 44/49 analyze errors to tokens.dart's dart:ui TextStyle import; closed AUD-008/AUD-009; opened AUD-012
- 2026-09-03 | c1 | Kit deployed, docs filled, pubspec retargeted, tokens + data + optics + rules + battle world written, 22 unit tests green
